# Atomic Orbital RDF Explorer

A Streamlit web version of the supplied `plotting_orbitals(1).ipynb`.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

## Deploy

Upload `app.py` and `requirements.txt` to a GitHub repository, then deploy the repository through Streamlit Community Cloud.
